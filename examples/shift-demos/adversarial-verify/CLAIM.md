# Claim under review (plausible but wrong — refute with raw evidence)

> `facts.txt` contains exactly **100** lines, and line **42** reads
> `fact 042: placeholder statement number 41`.

Both parts must be checked against the file itself (`wc -l`, `sed -n '42p'`);
a summary or a guess is not evidence.
