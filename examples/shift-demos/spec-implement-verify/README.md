# spec-implement-verify

`SPEC.md` is a one-line spec; `test_reverse_words.py` is the pinned oracle
(do not edit). Agent A implements `reverse_words.py` from the spec and
appends; agent B runs the oracle and approves on green. The implementation
file is gitignored — every run starts from the spec alone. ~4-6 turns.
