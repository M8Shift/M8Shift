# Spec (one line)

Implement `reverse_words(text)` in a new file `reverse_words.py`: return the
words of `text` in reverse order, joined by single spaces (words are the
non-empty chunks between whitespace).
